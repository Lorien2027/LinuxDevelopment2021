CFLAGS = -Wall -O0 -g

bin_PROGRAMS=rhasher
rhasher_SOURCES=rhasher.c

GENERATES = rhasher ChangeLog *.h *.in  *.log *.o  .deps compile config.status configure depcomp install-sh missing aclocal.m4 stamp-h1 Makefile test-driver autom4te.cache
clean:
	rm -rf $(GENERATES)